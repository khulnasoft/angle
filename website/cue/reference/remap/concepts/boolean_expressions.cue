remap: concepts: boolean_expressions: {
	title:       "Boolean expressions"
	description: """
		In VRL, Boolean [expressions](#expressions) resolve to a single Boolean value (either `true`
		or `false`). Boolean expressions can be of any complexity and have several uses in Angle,
		including specifying conditions for [routing](\(urls.angle_transforms)/route) and
		[filtering](\(urls.angle_transforms)/filter) events.
		"""
}
