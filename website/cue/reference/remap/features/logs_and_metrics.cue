remap: features: logs_and_metrics: {
	title:       "Logs and metrics"
	description: """
		VRL works with both [logs](\(urls.angle_log)) and [metrics](\(urls.angle_metric)) within Angle, making it
		usable for all [Angle events](\(urls.angle_data_model)).
		"""

	principles: {
		performance: false
		safety:      false
	}
}
