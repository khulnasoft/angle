---
# This file has been generated by `helm template angle angle/angle` from angle/templates/statefulset.yaml. Please re-run `make generate-kubernetes-manifests` rather than modifying this file manually.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: angle
  labels:
    app.kubernetes.io/name: angle
    app.kubernetes.io/instance: angle
    app.kubernetes.io/component: Aggregator
    app.kubernetes.io/version: "0.33.1-distroless-libc"
  annotations: {}
spec:
  replicas: 1
  podManagementPolicy: OrderedReady
  selector:
    matchLabels:
      app.kubernetes.io/name: angle
      app.kubernetes.io/instance: angle
      app.kubernetes.io/component: Aggregator
  serviceName: angle-headless
  template:
    metadata:
      annotations: {}
      labels:
        app.kubernetes.io/name: angle
        app.kubernetes.io/instance: angle
        app.kubernetes.io/component: Aggregator
        angle.khulnasoft.com/exclude: "true"
    spec:
      serviceAccountName: angle
      dnsPolicy: ClusterFirst
      containers:
        - name: angle
          image: "timberio/angle:0.33.1-distroless-libc"
          imagePullPolicy: IfNotPresent
          args:
            - --config-dir
            - /etc/angle/
          env:
          ports:
            - name: datadog-agent
              containerPort: 8282
              protocol: TCP
            - name: fluent
              containerPort: 24224
              protocol: TCP
            - name: logstash
              containerPort: 5044
              protocol: TCP
            - name: splunk-hec
              containerPort: 8080
              protocol: TCP
            - name: statsd
              containerPort: 8125
              protocol: TCP
            - name: syslog
              containerPort: 9000
              protocol: TCP
            - name: angle
              containerPort: 6000
              protocol: TCP
            - name: prom-exporter
              containerPort: 9090
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: "/angle-data-dir"
            - name: config
              mountPath: "/etc/angle/"
              readOnly: true
      terminationGracePeriodSeconds: 60
      volumes:
        - name: data
          emptyDir: {}
        - name: config
          projected:
            sources:
              - configMap:
                  name: angle
  volumeClaimTemplates:
