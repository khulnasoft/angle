---
# This file has been generated by `helm template angle angle/angle` from angle/templates/daemonset.yaml. Please re-run `make generate-kubernetes-manifests` rather than modifying this file manually.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: angle
  labels:
    app.kubernetes.io/name: angle
    app.kubernetes.io/instance: angle
    app.kubernetes.io/component: Agent
    app.kubernetes.io/version: "0.33.1-distroless-libc"
  annotations: {}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: angle
      app.kubernetes.io/instance: angle
      app.kubernetes.io/component: Agent
  minReadySeconds: 0
  template:
    metadata:
      annotations: {}
      labels:
        app.kubernetes.io/name: angle
        app.kubernetes.io/instance: angle
        app.kubernetes.io/component: Agent
        angle.khulnasoft.com/exclude: "true"
    spec:
      serviceAccountName: angle
      dnsPolicy: ClusterFirst
      containers:
        - name: angle
          image: "timberio/angle:0.33.1-distroless-libc"
          imagePullPolicy: IfNotPresent
          args:
            - --config-dir
            - /etc/angle/
          env:
            - name: ANGLE_SELF_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: ANGLE_SELF_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: ANGLE_SELF_POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: PROCFS_ROOT
              value: "/host/proc"
            - name: SYSFS_ROOT
              value: "/host/sys"
            - name: ANGLE_LOG
              value: "info"
          ports:
            - name: prom-exporter
              containerPort: 9090
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: "/angle-data-dir"
            - name: config
              mountPath: "/etc/angle/"
              readOnly: true
            - mountPath: /var/log/
              name: var-log
              readOnly: true
            - mountPath: /var/lib
              name: var-lib
              readOnly: true
            - mountPath: /host/proc
              name: procfs
              readOnly: true
            - mountPath: /host/sys
              name: sysfs
              readOnly: true
      terminationGracePeriodSeconds: 60
      volumes:
        - name: config
          projected:
            sources:
              - configMap:
                  name: angle
        - name: data
          hostPath:
            path: "/var/lib/angle"
        - hostPath:
            path: /var/log/
          name: var-log
        - hostPath:
            path: /var/lib/
          name: var-lib
        - hostPath:
            path: /proc
          name: procfs
        - hostPath:
            path: /sys
          name: sysfs
